package com.unipi.alexandris.android.echotrialsonandroid.data;

/**
 * Enum representing different causes of player death.
 * Used to determine appropriate death effects, respawn behavior, and game logic.
 */
public enum DeathCause {
    SPIKE,
    FALL,
    TAP,
    SUFFOCATION
} 